<?xml version="1.0" encoding="utf-8"?>
<!-- Lecture paragraphs (L tab) + How-style three-column host (Learning / Practice / Test / V). -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ScrollView
        android:id="@+id/lecture_learning_scroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <TextView
            android:id="@+id/lecture_paragraph_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="16dp"
            android:paddingTop="12dp"
            android:paddingEnd="16dp"
            android:paddingBottom="24dp"
            android:textColor="#1F2937"
            android:textSize="18sp" />
    </ScrollView>

    <FrameLayout
        android:id="@+id/lecture_threecol_host"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

        <include layout="@layout/layout_threecol_content" />
    </FrameLayout>
</FrameLayout>
