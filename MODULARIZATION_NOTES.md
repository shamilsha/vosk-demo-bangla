# Modularization & memory notes

- **Memory:** Cursor/IDE can crash when memory goes above ~4.5GB. Work in small steps; avoid loading or editing huge files in one go.
- **Already extracted:** `SentenceAdapter.kt`, `MainActivityTypes.kt`, `SimpleSentenceUtils.kt`, `DrawerTopicBuilders.kt`, `SVOLessonPaths.kt`, `StringUtils.kt`, `LessonFileParsers.kt`, `ConversationScriptLoader.kt`, `SVODataLoaders.kt`, `UriUtils.kt`, `DiagramHtml.kt`, `TableHtmlBuilder.kt`, `MatchNormalizer.kt`, `SentenceMatchUtils.kt`, `SvoSpannableUtils.kt`, `SubtopicInstructions.kt`, `DialogUtils.kt`, `ControlBarUtils.kt`, `PronunciationLessonData.kt`, `VerbLessonData.kt`; plus Recognizer, FlowLayout, ConveyorBeltView, VoskEnInRecognizer, AppClass.
- **Layouts:** Most screens use `layout_*` or `content_*`; item/drawer use `item_*` / `drawer_*`. No rename needed for "layout" suffix.
- **Lesson shell:** `layout_lesson_base.xml` (template) + `layout_lesson_mode_bar.xml` (shared magenta strip) + `LESSON_BASE_LAYOUT.md` + `assets/diagrams/lesson-base-layout-mockup.html`. New lessons copy the base layout; preposition follows the same hierarchy.
- **Next small steps:** Extract one helper or one layout-related block at a time from MainActivity (e.g. table HTML builder, drawer builder, or a single ContentLayout handler) into a new file; test build after each step.
- **Two kinds of refactor:** (1) **Extraction** = move a function to another file and delete it from MainActivity → reduces MainActivity.kt size. (2) **Inline replacement** = replace string literal with helper call (e.g. svTripleFileName) → does not reduce line count. Prefer extractions to shrink the file.

**Step log (23 steps so far):** Step 1 (note). Step 2–8: extractions. Step 9–12: svTripleFileName (no size reduction). Step 13: sanitizeListName → StringUtils. Step 14–17: LessonFileParsers. Step 18: ConversationScriptLoader. Step 19: SVODataLoaders. Step 20: `getDisplayName` → UriUtils.kt. Step 21: `loadSvWordsData`, `loadSvIngData` → SVODataLoaders.kt. Step 22: ribbon loaders → SVODataLoaders.kt. Step 23: `loadTripleDataINegative` → SVODataLoaders.kt. Step 24: `loadTripleDataIQuestion` → SVODataLoaders.kt. Step 25: `loadTripleDataIQuestionNegative` → SVODataLoaders.kt. Step 26: `loadTripleDataYouQuestion` → SVODataLoaders.kt. Steps 27–31: YouQuestionIng, YouQuestionNegative, SubjectNegative, SubjectQuestion, SubjectQuestionNegative → SVODataLoaders.kt. Steps 32–36: Cont, ContQuestion, ContNegative, ContQuestionNegative, PastQuestion → SVODataLoaders.kt. Steps 37–41: PastNegative, PastQuestionNegative, PastCont, PastContQuestion, PastContNegative → SVODataLoaders.kt. Steps 42–49 done (batch): all remaining `loadTripleData*` → SVODataLoaders.kt — PastContQuestionNegative, FutureQuestion, FutureNegative, FutureQuestionNegative, FutureCont, FutureContQuestion, FutureContNegative, FutureContQuestionNegative; removed `svTripleSubjectOrder` from MainActivity (~285 lines removed). **All loadTripleData* extractions complete.** Steps 50–58: getLessonInfoBengali, parseShortIVsLongEeFile → LessonFileParsers; DiagramHtml; TableHtmlBuilder; MatchNormalizer; splitIntroductionSegments → StringUtils. Steps 59–65: SentenceMatchUtils.kt (edit distance, placeholder match, alternatives); buildTableTitle → TableHtmlBuilder.kt. Step 66 done: `buildTableHtml` (interactive table, silent letter, card style) → TableHtmlBuilder.kt (~165 lines removed from MainActivity). Steps 67–68 done: `loadIncorrectLessonListFromFile`, `parseLessonFileWithInfo` → LessonFileParsers.kt (~70 lines removed). Steps 69–70 done: `PaddedBackgroundSpan`, `makeSvoSpannable`, `makeSvoDescription`, `makeExpectedAndHeardSpannable`, `makeExpectedAndYouSaidSpannable` → SvoSpannableUtils.kt (~120 lines removed). Step 71 done: `saveIncorrectLessonListToFile` → LessonFileParsers.kt (~25 lines removed). Step 72 done: `getSubtopicInstruction` → SubtopicInstructions.kt (~88 lines removed). Step 73 done: `escapeForHtmlBody`, `makeIntroductionHtml` → StringUtils.kt (~12 lines removed from MainActivity). Step 74 done: `buildDrawerItems` → DrawerTopicBuilders.kt (~10 lines removed). Step 75 done: `usesControlActions` → MainActivityTypes.kt (top-level; ~13 lines removed from MainActivity). Step 76 done: `getTopicProgressSummary` → DrawerTopicBuilders.kt (~7 lines removed). Step 77 done: `ribbonItemHeightPx` → MainActivityTypes.kt (~6 lines removed). Step 78 done: `showDrawerInfoDialog` → DialogUtils.kt (`showInfoDialog`; ~11 lines removed). Step 79 done: `setControlStartStopButton`, `setControlPauseResumeButton` → ControlBarUtils.kt (~12 lines removed). Step 80 done: `alphabetBengaliPronunciation` → MainActivityTypes.kt (~14 lines removed). Step 81 done: removed duplicate `private data class Subtopic` from MainActivity (use MainActivityTypes.Subtopic). Step 82 done: removed duplicate `private data class VerbRow` from MainActivity (use MainActivityTypes.VerbRow). Step 83 done: removed duplicate `private data class SvEntry` from MainActivity (use MainActivityTypes.SvEntry). Step 84 done: `getPronunciationLessons` → PronunciationLessonData.kt (~200 lines removed). Step 85 done: `getVerbData`, `getTenseData` → VerbLessonData.kt (~275 lines removed). Step 86 done: `getTopicList(assets)` → DrawerTopicBuilders.kt (~92 lines removed). Step 87 done: `buildLessonFromVerb`, `buildLessonFromTense` → VerbLessonData.kt (~12 lines removed). Step 88 done: `getContentLayoutResId(layout)` → MainActivityTypes.kt (~17 lines removed). Step 89 done: `LIST_FILE_SUFFIX` → StringUtils.kt (~1 line removed); added BRACE_ISOLATION.md with instructions to comment out blocks to isolate the extra `{`. Step 90 done: `INCORRECT_LESSON_SUFFIX` → LessonFileParsers.kt (public const); use LessonFileParsers.INCORRECT_LESSON_SUFFIX in MainActivity (~2 lines removed). Step 91 done: `PERMISSION_CODE` → MainActivityConstants.kt as `PERMISSION_REQUEST_CODE`; removed MainActivity companion object (~4 lines removed). Step 92 done: `permList` → MainActivityConstants.kt as `INITIAL_PERMISSIONS` (~3 lines removed). Step 93 done: `tryAgainListenFallbackDelayMs` → MainActivityConstants.kt as `TRY_AGAIN_LISTEN_FALLBACK_DELAY_MS` (~2 lines removed). Step 94 done: `channelConfig`, `audioFormat` → MainActivityConstants.kt as `AUDIO_CHANNEL_CONFIG`, `AUDIO_FORMAT_PCM_16` (~5 lines removed). Step 95 done: `minSamplesBeforeRecognizer` → MainActivityConstants.kt as `MIN_SAMPLES_BEFORE_RECOGNIZER` (~4 lines removed). Step 96 done: literal 1600 → MainActivityConstants.kt as `MIN_RECORDING_BUFFER_SAMPLES` (~1 line removed). Step 97 = next. **Brace:** Commenting out the full body of `handleSubtopicAction` (lines 2442–2913) made "Missing '}'" go away → the extra `{` is inside that function; see BRACE_ISOLATION.md. Refactoring helps isolate it: each extraction shrinks MainActivity, so when the missing brace is fixed, the remaining code is smaller and the error is easier to locate.
